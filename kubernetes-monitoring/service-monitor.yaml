apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nginx-monitoring
  labels:
    team: nginx
spec:
  selector:
#    matchLabels:
      app: nginx-exporter
  endpoints:
  - port: nginx-exp